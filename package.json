{
  "name": "client-scanner-resi",
  "version": "1.0.0",
  "description": "Aplikasi Client Scanner Resi",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step required for static HTML'",
    "sync": "cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}